pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        echo 'build completed'
      }
    }
  Stage('deploy') {
      steps {
        echo 'deploy completed'
      }
    }

  }
}